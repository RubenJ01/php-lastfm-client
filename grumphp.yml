grumphp:
    tasks:
        phpcs:
            standard: ["PSR12"]
            whitelist_patterns:
                - /^src\/(.*)/
                - /^tests\/(.*)/
        phpstan:
            configuration: phpstan.neon
            memory_limit: "-1"
            ignore_patterns:
                - "configure.php"
        phpunit: ~
